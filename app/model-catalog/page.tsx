import ModelCatalog from '@/components/models/ModelCatalog';
export const metadata={title:'Model catalog — Counterfactual Worlds',description:'Local CC0 scene art with previews, usage guidance and provenance.'};
export default function Page(){return <ModelCatalog/>;}
