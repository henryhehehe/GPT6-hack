import { sqliteTable,text,integer,index } from 'drizzle-orm/sqlite-core';
export const classrooms=sqliteTable('classrooms',{id:text('id').primaryKey(),teacherToken:text('teacher_token').notNull(),inviteToken:text('invite_token').notNull(),world:text('world').notNull(),state:text('state').notNull(),version:integer('version').notNull().default(1),lesson:text('lesson').notNull(),createdAt:text('created_at').notNull()});
export const students=sqliteTable('students',{id:text('id').primaryKey(),classId:text('class_id').notNull().references(()=>classrooms.id),token:text('token').notNull(),state:text('state').notNull(),revision:integer('revision').notNull().default(1)},t=>[index('students_class_idx').on(t.classId)]);
